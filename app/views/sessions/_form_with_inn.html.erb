<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%# <%= form_with(url: "/login/inn}", method: :post, local: true) do |form| %> %>
    <%= form_with scope: :session, url: create_session_path("inn") do |f| %>
      <%= f.label :inn, "ИНН" %>
      <%= f.text_field :inn, class: "form-control" %>

      <%= f.label :password, "Пароль" %>
      <%= f.password_field :password, class: "form-control" %>

      <%= f.label :remember_me, class: "checkbox inline" do %>
        <%= f.check_box :remember_me %>
        <span>Запомнить меня</span>
      <% end %>

      <%= f.submit "Войти", class: "btn btn-primary"%>
    <% end %>

    <p>Ещё не зарегистрированы? <%= link_to "Регистрация", signup_path('user') %></p>
  </div>
</div>